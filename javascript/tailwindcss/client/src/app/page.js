import Container from '@/components/Container';

const Page = () => {
    return (
        <Container>
            <div className={''}>client</div>
        </Container>
    );
};

export default Page;